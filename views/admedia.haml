%h2 Ad Media List
- if @connected
  %p zanox Connect successful
- else
  %p zanox Connect not successful

- if @programApplications.size > 0
  %ul
    - @programApplications.each do |programApplication|
      %li
        =programApplication.program['id'].to_s + ' ' + programApplication.status
- else
  %p
    blabla
    
=@programApplications[0].inspect

